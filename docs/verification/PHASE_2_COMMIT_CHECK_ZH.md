# 第二期候选实现提交前核查

2026-09-12。用户接受当前三段预览方向并要求 commit & push，本次冻结候选实现、测试及文档。该确认不代表音色或第二期整体质量验收通过。

## 本轮检查

- 单元及新增媒体测试：54 passed。JUnit：本机 `evidence/phase2_commit_check_001/unit-media.xml`。
- 第一期相关媒体回归：13 passed，覆盖规范化、独立渲染预期、分数帧率、多切点和片尾。临时文件与结果放到挂载盘后重跑通过，日志和 JUnit：`/Volumes/备份/mlvideo-phase2-commit-check-20260912_004/`。
- 真实 MySQL 重试/血缘及第一期完整媒体配方：2 passed。待 MySQL 恢复 healthy 并实际连接成功后执行，日志和 JUnit：`/Volumes/备份/mlvideo-phase2-commit-check-20260912_005/`。
- 合计 69 项通过。已暂存 Python 致命错误检查、JSON Schema 定义校验、本地文档链接及 Git diff 空白检查通过。
- 暂存范围检查通过，没有提交 `.env`、本地数据库配置、模型权重、下载媒体或运行证据大文件。

## 环境失败记录

初次回归遇到本机 ENOSPC，后发现 Docker daemon 未运行。部分媒体验证虽已执行，复制结果至本机时仍因空间不足失败，因此未将该轮标为通过。已保留本轮错误日志，并仅清理/迁移完成的 pytest 临时目录；未删除原始媒体、模型或历史数据库/业务证据。

磁盘可用空间恢复后，启动现有 Docker/MySQL 服务，未清空或重建数据卷。首次数据库重试在服务尚未就绪时连接中断，保留记录；确认 healthy 和 SQL 查询成功后，使用新的输出目录完成最后两项回归。

## 下一阶段

当前仍是第二期收尾，第三期尚未开始验收。先解决可靠说话人分组/参考音选择、音色与内容/尾音复核、真实整片候选及完整质量验证。官方 CosyVoice3 CPU FP32/8 线程、一次加载连续生成和参考缓存已有基准；生产常驻 worker 尚未接入。

完成交接后，再按 [第三期 kickoff](../kickoffs/PHASE_3_KICKOFF_ZH.md) 推进适用场景的字幕清理/样式、完整 QA、长片、备份恢复和发布。保留原英文的素材继续使用用户确认的画面内中文布局，不自动擦除原英文。

模型、媒体、原始请求/响应和本机运行证据受 `.gitignore` 排除，需连同本机/挂载盘数据保留；仅克隆 Git 仓库不会取得这些文件。
