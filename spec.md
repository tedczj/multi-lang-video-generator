# 视频生成固定规范

本文件记录用户已确认的产品要求，适用于正式流水线、试制脚本和临时成片。实现、修改和验收前必须阅读；不能为方便实现而偏离。新的明确用户指令优先，变更后同步本文件与测试。

## 画面与字幕

- 保持原视频的显示尺寸和画面比例；不裁切原画面，不增加黑边、字幕底栏或其他画布区域。例如 1920×1080 原片仍输出 1920×1080。
- 保留原片英文字幕；中文字幕紧邻**当前实际英文字幕块**，优先放在其下方，下方空间不足时放在上方。
- 根据当前帧或经核对的字幕页面测量英文字幕框。不得用全片统一底部坐标或全部字幕框的并集替代当前字幕的位置。
- 中文需考虑实际字形、描边、换行和安全边距；不得遮盖英文、越界或被裁掉。上下都放不下时调整字号/换行并复核，不得加底栏兜底。
- 中文字幕直接叠加在画面上，使用必要的文字描边保证可读性；不重绘一份英文字幕，也不在成片中添加模型、角色 ID、技术状态等调试标识。
- 无法可靠定位当前英文字幕时保留待复核状态，不能猜位置后宣称通过。
- 中文停帧所用画面应保留对应英文字幕。如果停帧点的英文已消失，可选同一句最近的带字幕源帧作为停帧图，记录帧号；不得改变原声播放过程、停帧时长和配音时间。

## 声音与时序

- 保留完整英文原声及原始画面时序，在既定安全位置停帧播放中文。
- 引号内台词归说话角色；`said/asked + 角色名`、`Roxy laughed` 等叙述归旁白。角色台词中的转述（如 `She said you can leave school…`）不能仅凭 `said` 拆成旁白。
- 角色与旁白混合段应分别生成对应声音；英文没有安全间隙时，可连续播完相关原声，再依次播放对应中文，不能切断原声。
- 只修字幕布局时复用已经固定的音频、角色选择和时间线，不重新合成、裁剪、变速或改变配音。能直接复制音轨时优先复制，并校验一致性。
- 用户批准“先做一版”代表允许试制，不等于所有参考、音色或整片质量正式通过。

## 标注与版本

- 译文与备注使用独立输入框、独立用途。译文支持异步生成与人工修改，异步结果不得覆盖用户改写。
- 备注停止输入后自动保存，离开输入框立即保存，不要求点击确认；失败要显示原因并保留草稿。
- 保留源素材、输入绑定、人工意见和已交付版本。修改创建新版本，不覆盖上一版。
- 不把未选择、待试听、技术检查通过或模型建议自动升级为人工通过；无需填写审核人姓名。

## 必须执行的检查

1. 自动核对输出显示尺寸与源视频一致，字幕画布尺寸也一致；拒绝非零 `footer_height`。
2. 自动核对中文字幕框在画面内、与当前英文框不相交；下方可容纳时应优先下方，不能靠新增底栏通过边界检查。
3. 根据原英文字幕出现的实际页面，抽取原声阶段、停帧阶段、一行/多行字幕、上下方切换和首尾画面做视觉复核；保留逐页定位证据。
4. 仅布局修复时核对配音文件哈希、音轨数据、时长、帧数与帧时间戳保持一致。
5. 分别报告实现完成、技术检查、视觉抽查、人工试听和正式验收状态；未完成的检查不得标记 PASS。

## 资源约束

- 大任务前检查磁盘空间并保留既有安全余量。空间不足时使用用户允许的试制编码方式，不改变上述画面/字幕规则。
- 只清理明确授权或本轮可重建的临时文件；保留已交付成片、参考、逐句音频和证据。
