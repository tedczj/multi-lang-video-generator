# 单 URL 自动成片：整集执行记录

## 交付与范围

- 输入：https://www.youtube.com/watch?v=73XAgADpBqY
- 最终 MP4：`/Volumes/备份/视频成片_请勿清理/西游记_整集_英中紧接_20260918_v2.mp4`
- 工作目录：`/Volumes/备份/视频生成工作数据_请勿清理/jttw-20260917`
- 最终渲染检查：工作目录下 `render_006cc27658274c8b9522c87d2957a05e/`
- 1920×1080，10,386 个原始帧，646.804833 秒，66 个英中句组、94 个实际 CosyVoice3 配音片段、78 个字幕页面。
- 新交付只使用 MP4/H.264/yuv420p/AAC，moov 位于 mdat 前，faststart 已独立检查（`container.json`）。

入口与详细使用：[GENERATE_VIDEO_ZH.md](../../docs/GENERATE_VIDEO_ZH.md)。

实际整片最终调用：

```bash
./scripts/generate_video.sh 'https://www.youtube.com/watch?v=73XAgADpBqY' \
  --work '/Volumes/备份/视频生成工作数据_请勿清理/jttw-20260917' \
  --output '/Volumes/备份/视频成片_请勿清理/西游记_整集_英中紧接_20260918_v2.mp4' \
  --resume
```

开发过程中先分步执行真实下载、Whisper/VAD、说话人分析、OCR、Codex 脚本与 CosyVoice3，再由单 URL 入口核对绑定并完成断点续跑与全片输出。**这不是从空目录单次启动的真实冷运行**。从空目录的完整步骤调度、恢复及拒绝篡改另用明确的模型替身和真实 FFmpeg/PyAV 编解码做集成测试；实际整片没有用提示音、Tingting 或替身音频。

## 自动核对

`tests.log/xml`：124 passed，覆盖新分段/字幕/恢复行为及旧时间线零额外间隔回归。新增文件 Ruff check 与 Git diff check 通过。

`media-checks.json` 记录：

- 英文原声→中文顺序，每段中文开始采样恰等于英文结束点，新增英中间隔为 0。
- 编码前每个源 PCM 前段、中文、源 PCM 尾段逐段一致，原声完整保序保留。
- 全量解码，10,386 帧全部保序，无新增 hold；帧时间戳误差及视频尾时长误差为 0 个采样。
- H.264/AAC、显示尺寸、AAC 尾部编码 padding 检查通过；AAC 相对 RMS 差约 0.037，有损编码不宣称字节相等。
- 场景像素采用逐帧中央 ROI 缩略图 MAE 容差检查，不能据此声称全画面无损或人工观感通过。

`resume.log`：全部完成阶段正确复用；正式回归测试会篡改成片并验证恢复拒绝，不能静默使用变更后的文件。

## 字幕与音频独立性

初次视觉抽查发现，连续发声合并的句组把整组中文放到了每个英文页。已改为按当前英文页选择对应完整句子，保留该句的台词＋归属旁白，不提前显示后续台词。

v2 仅改了 14 页的中文文本范围。`layout-only-checks.json` 证明组合 PCM、AAC 工作音频哈希、全部帧映射/时间戳、配音排布均与 v1 相同；未重新合成声音。原 v1 和其截图保留为历史证据，最终使用 v2。

已视觉查看 `contact.jpg` 的首尾、标题、旁白、台词、长句、两处补回旁白，以及 `page-scope-fixed.jpg` 对应原先过长的瀑布页面。中文位于实际英文下方，未加画布/底栏，未重绘英文。没有把少量抽查标成全部 78 页的人工作品验收。

## 内容恢复及仍待复核项

- OCR 揭示了 Whisper 整段识别漏掉的两处归属旁白。重新识别原声小窗口后，补回 tall monkey / short monkey 的 said 部分，并核对 gibbon。过程在 `asr-refinement.json`，音频仍是依据，OCR 提示本身不等于真值。
- 原自动聚类给出 19 个标签，不能直接当角色身份。当前使用台词结构产生 14 个语义角色候选，参考音均来自本片相应片段；身份/音色仍为 REVIEW。
- `review.json` 列出 6 个低于 0.4× 的慢速句组、3 个前导能量待复核片段、1 个辅助中文 ASR 空结果和 9 个短于 3 秒的参考角色。没有加速/截短配音来隐藏这些问题。
- 片头品牌声和片尾 “you” ASR 候选未获独立 VAD 支持，未据其生成中文；全部原音保留。
- 模型自己的前导噪声/弱声不等于程序插入静音；未随意裁掉可能的起始发声。零人为间隔已实现，主观听感仍需试听。

实现完成、技术检查通过；**真人试听、角色身份、语义/自然度、正式验收仍为 REVIEW**。目录式入口不回写 Studio/MySQL 队列；未宣称整个工作台或所有 Phase 2/3 验收均完成。完整媒体、模型请求、逐句音频和原始识别证据保留在本机/NAS，Git 中只提交代码、说明与小型证据。
